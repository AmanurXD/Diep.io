// Maximum number of active entities (players + bullets + shapes)
// 10,000 allows roughly: 100 players, 2000 shapes, 7900 bullets.
export const MAX_ENTITIES = 10_000;

// Null value for entities (e.g. parent ID when no parent exists)
export const NULL_ENTITY = -1;